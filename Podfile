platform :ios, '13.0'

# For library development: this Podfile is optional
# To test the library locally without creating an app project:
# 
#   pod install
#   
# Then include in your app's Podfile:
#   pod 'PredictionKeyboard', :path => '../PredictionKeyboardClean'
#
# For an example test app, use the Example/ subdirectory

target 'PredictionKeyboardExample' do
  use_frameworks!
  
  # Local pod for development
  pod 'PredictionKeyboard', :path => '.'
  
  # Required dependencies
  pod 'Realm', '~> 10.0'
  
end
